import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: '图片转 Base64 | Tools | Vfan Lee',
  description: '将图片文件转换为 Base64 编码字符串，支持拖拽上传，支持 JPEG、PNG、GIF、WebP、BMP、SVG 等格式。',
}

export { default } from './_page'
